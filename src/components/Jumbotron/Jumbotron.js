import React from 'react';
import './Jumbotron.css';

const Jumbotron = () => (

  <div className = 'jumbotron'>
    <h1>InstruMemory</h1>
    <h4>Click on an instrument to earn points, but don't click it more than once!</h4>
  </div>
);

export default Jumbotron;
